export type { Theme, Breakpoints } from './config';
export { useTheme, StyleProvider } from './config';

export { getStyleElement } from './core/inject-web-styles';
export { createStyleSheet } from './core/create-stylesheet';
